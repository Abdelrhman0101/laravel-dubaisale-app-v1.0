# تغيير نوع البيانات لحقول Car Sales

## ✅ التعديل المطلوب

تم تغيير نوع البيانات للحقول التالية في جدول `car_sales_ads` من **`integer`** إلى **`string`**:

1. ✅ `engine_capacity` (سعة المحرك)
2. ✅ `seats_no` (عدد المقاعد)
3. ✅ `doors_no` (عدد الأبواب)
4. ✅ `cylinders` (عدد الأسطوانات)

---

## 📁 الملف المنشأ

**Migration**: 
```
database/migrations/2026_01_08_211100_change_car_sales_fields_to_string.php
```

---

## 🛠️ الأمر المطلوب

```bash
php artisan migrate
```

هذا الأمر سيقوم بتحويل الحقول الأربعة من `integer` إلى `string` في قاعدة البيانات.

---

## 📝 ملاحظات

### ✅ الإيجابيات:
- **مرونة أكبر**: يمكن الآن إدخال قيم مثل "2.5L" أو "Turbo" للمحرك
- **تجنب أخطاء التحويل**: بعض القيم قد لا تكون أرقام صحيحة فقط
- **سهولة العرض**: عدم الحاجة لتحويلات إضافية في Frontend

### ⚠️ نقاط يجب الانتباه لها:
1. **البيانات الحالية**: إذا كان هناك بيانات موجودة، سيتم تحويلها تلقائياً إلى string
2. **Validation**: يُنصح بإضافة Validation للتأكد من صحة القيم المدخلة:
   ```php
   'engine_capacity' => 'nullable|string|max:50',
   'seats_no' => 'nullable|string|max:10',
   'doors_no' => 'nullable|string|max:10',
   'cylinders' => 'nullable|string|max:10',
   ```
3. **الفرز والتصفية**: إذا كنت تستخدم هذه الحقول في الفرز أو التصفية، قد تحتاج لتعديلات إضافية

---

## 🔄 التراجع عن التغيير

إذا أردت إعادة الحقول إلى `integer`، استخدم:

```bash
php artisan migrate:rollback --step=1
```

---

## 📊 مثال على القيم الممكنة بعد التعديل

### قبل التعديل (integer):
```json
{
  "engine_capacity": 2500,
  "seats_no": 5,
  "doors_no": 4,
  "cylinders": 6
}
```

### بعد التعديل (string):
```json
{
  "engine_capacity": "2.5L Turbo",
  "seats_no": "5",
  "doors_no": "4",
  "cylinders": "V6"
}
```

---

**تم بنجاح! ✅**
