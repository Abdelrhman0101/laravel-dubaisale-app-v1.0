# 🚀 نظام الإبلاغ عن الإعلانات - ملخص سريع

## ✅ تم التنفيذ بنجاح!

تم إنشاء نظام كامل للإبلاغ عن الإعلانات يتضمن:

---

## 📁 الملفات المُنشأة

1. **Migration**: `database/migrations/2026_01_08_204400_create_ad_reports_table.php`
2. **Model**: `app/Models/AdReport.php`
3. **Controller**: `app/Http/Controllers/Api/AdReportController.php`
4. **Routes**: تم إضافتها في `routes/api.php`
5. **دليل الاستخدام**: `AD_REPORTS_GUIDE.md`

---

## 🎯 Endpoints الرئيسية

### للجميع (بدون تسجيل):
```
POST   /api/reports                    // إرسال بلاغ
GET    /api/reports/reasons            // أسباب الإبلاغ
GET    /api/reports/ad-types           // أنواع الإعلانات
```

### للمستخدمين المسجلين:
```
GET    /api/reports/my-reports         // بلاغاتي
```

### للأدمن فقط:
```
GET    /api/admin/reports              // جميع البلاغات
GET    /api/admin/reports/stats        // الإحصائيات
GET    /api/admin/reports/{id}         // تفاصيل بلاغ
PUT    /api/admin/reports/{id}         // تحديث حالة بلاغ
DELETE /api/admin/reports/{id}         // حذف بلاغ
```

---

## 📝 مثال سريع: إرسال بلاغ

### Request:
```http
POST /api/reports
Content-Type: application/json

{
  "ad_type": "car_sale",
  "ad_id": 123,
  "reason": "spam",
  "description": "إعلان مكرر"
}
```

### Response:
```json
{
  "success": true,
  "message": "تم إرسال البلاغ بنجاح. سيتم مراجعته في أقرب وقت.",
  "data": {
    "report_id": 1,
    "status": "pending",
    "created_at": "2026-01-08 20:45:30"
  }
}
```

---

## 🔑 أهم الحقول

### Request Body (إرسال بلاغ):
| الحقل | الوصف | مطلوب | القيم المتاحة |
|------|-------|-------|---------------|
| `ad_type` | نوع الإعلان | ✅ | car_sale, car_rent, restaurant, job, etc. |
| `ad_id` | رقم الإعلان | ✅ | رقم صحيح |
| `reason` | سبب البلاغ | ✅ | spam, inappropriate, misleading, etc. |
| `description` | وصف تفصيلي | ❌ | نص (حتى 1000 حرف) |

### أسباب الإبلاغ:
- `inappropriate` → محتوى غير لائق
- `spam` → إعلان مزعج أو تكراري
- `misleading` → معلومات مضللة
- `duplicate` → إعلان مكرر
- `fraud` → احتيال أو نصب
- `wrong_category` → قسم خاطئ
- `other` → أخرى

### حالات البلاغ:
- `pending` → معلق (في انتظار المراجعة)
- `reviewed` → تمت المراجعة
- `resolved` → تم الحل
- `rejected` → مرفوض

---

## 🛠️ خطوات التشغيل

### 1. تشغيل Migration:
```bash
php artisan migrate
```

### 2. جاهز للاستخدام! 🎉

---

## 📊 مثال: الأدمن يحدث حالة البلاغ

### Request:
```http
PUT /api/admin/reports/1
Authorization: Bearer {admin_token}
Content-Type: application/json

{
  "status": "resolved",
  "admin_note": "تم حذف الإعلان المخالف"
}
```

### Response:
```json
{
  "success": true,
  "message": "تم تحديث البلاغ بنجاح",
  "data": {
    "id": 1,
    "status": "resolved",
    "admin_note": "تم حذف الإعلان المخالف",
    "reviewed_at": "2026-01-08 21:30:00"
  }
}
```

---

## 🔒 الصلاحيات

| العملية | الصلاحية المطلوبة |
|---------|-------------------|
| إرسال بلاغ | الجميع (حتى بدون تسجيل) ✅ |
| عرض بلاغاتي | مستخدم مسجل 🔐 |
| إدارة البلاغات | أدمن فقط 👑 |

---

## 📖 دليل كامل

لمزيد من التفاصيل، راجع ملف: **`AD_REPORTS_GUIDE.md`**

---

## ✨ مميزات النظام

✅ إبلاغ سهل وسريع  
✅ لا يتطلب تسجيل دخول (اختياري)  
✅ التحقق التلقائي من وجود الإعلان  
✅ لوحة تحكم كاملة للأدمن  
✅ إحصائيات مفصلة  
✅ Pagination لجميع القوائم  
✅ تصفية متقدمة للبحث  

---

**تم بحمد الله! 🎉**

الآن يمكنك تشغيل `php artisan migrate` والبدء في استخدام النظام فوراً.
